import { createSlice } from "@reduxjs/toolkit";

const initialState = {};

const counterSlicer = createSlice({
  name: initialState,
  initialState,
  reducers: {},
});

export const {} = counterSlicer.actions;
export default counterSlicer.reducer;
